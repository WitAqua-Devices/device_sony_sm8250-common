<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.lineageos.settings.felica"
          android:versionCode="1"
          android:versionName="1.0"
          android:sharedUserId="android.uid.system">

    <application
        android:label="@string/app_name">

        <activity
                android:name=".FelicaSettingsActivity"
                android:exported="false"
                android:theme="@style/Theme.SubSettingsBase.Expressive"
                android:label="@string/felica_setting_header">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS"/>
            </intent-filter>
            <meta-data android:name="com.android.settings.category"
                       android:value="com.android.settings.category.ia.device" />
            <!-- DashboardFeatureProviderImpl adds the order of
                 dashboard_tile_placeholder (-8 here) to this, so 1 ties with
                 the NFC entry at -7 and the two then sort by title, which
                 leaves this directly under NFC. A tile without an order does
                 not get one set at all and ends up near the bottom. -->
            <meta-data android:name="com.android.settings.order"
                       android:value="1" />
            <meta-data android:name="com.android.settings.summary"
                       android:resource="@string/felica_setting_summary"/>
        </activity>

    </application>
</manifest>
